@model List<NotesApp.Models.Notes.TagViewModel>

<div>
    <a href="/Tags/Add">Create tag</a>
</div>
<div>
    @foreach (var tag in Model)
    {
        <div>
            @tag.Name
            @if (tag.IsAllowedToDelete)
            {
                <a href="/Tags/Remove?id=@tag.Id" title="Delete" style="margin-left:8px;">✕</a>
            }
        </div>
    }
</div>