@model List<NotesApp.Models.Notes.CategoryViewModel>

<div>
    <a href="/Categories/Add">Create category</a>
</div>
<div>
    @foreach (var category in Model)
    {
        <div>
            @category.Name
            @if (category.IsAllowedToDelete)
            {
                <a href="/Categories/Remove?id=@category.Id" title="Delete" style="margin-left:8px;">✕</a>
            }
        </div>
    }
</div>